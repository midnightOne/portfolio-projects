# Handoff — 2026-07-08 (review last session, then resume Block C)

You are resuming a long-running architecture-migration project on the Portfolio
repo. Your job this session has TWO phases: (1) critically REVIEW the last
session's work from Git, then (2) RESUME Phase 4 Block C. Read `CLAUDE.md` at the
repo root FIRST, then the specs it points to. Do not trust this prompt over the
code — verify claims against the diff.

## Repo / ground rules

- Working repo: `C:\Users\imidn\Portfolio\portfolio-projects` (the outer
  `Portfolio` dir is NOT the repo). Branch:
  `claude/semantic-content-management-review` (this is STAGING). NEVER merge into
  or touch `main` (D1 amended) — it's the deployable Vercel fallback. Commit per
  logical unit; do not push unless asked.
- Local dev DB = WSL Postgres (keepalive gotcha in CLAUDE.md: WSL idles out and
  drops Postgres mid-session → restart `sleep infinity` + `service postgresql
  start`). Use `127.0.0.1`, never `localhost`.
- Specs are authoritative (`.kiro/specs`). Decisions in
  `00-overview/decision-registry.md` arbitrate. Never hardcode model IDs/prices.
- Definition of done (D46): type-check + build + test + `check:*` + the feature's
  e2e path exercised live. Jest baseline is ~34–35 FAILING suites, ALL
  pre-existing Phase 5.3 legacy debt — do not regress it, do not fix it. The
  OpenAI adapter suite fails 8/4 on mic-permission (jsdom, environmental).
- NEVER run `npm run build` / `rm -rf .next` / full `npm test` while a dev server
  is serving — it corrupts `.next`. `type-check`, `check:gateway`, `check:specs`,
  and targeted `npx jest <file>` are safe.
- Admin login for live verification (NextAuth credentials, in the preview browser
  context): `fetch('/api/auth/csrf')` → POST `csrfToken` + `username=admin` +
  `password=admin2025` + `json=true` to `/api/auth/callback/credentials`.

---

## Phase 1 — Review the last session (3 commits on top of `1078613`)

Review: `git diff 1078613..ad18d82` (commits `867b645`, `48fd3e3`, `ad18d82`).
These were owner-driven follow-ups from live Gemini-Live homepage testing —
observability + a tool-call latency UX fix + an admin transcript browser. All
`type-check`/`check:gateway`/`check:specs` green and browser-verified, BUT the
following are worth a skeptical second pass:

1. **`_postConversationLog()` refactor**
   (`src/lib/voice/IConversationalAgentAdapter.ts` + `GoogleLiveAdapter`,
   `OpenAIRealtimeAdapter`, `ElevenLabsAdapter`). Added a shared base-adapter
   helper that POSTs to `/api/ai/conversation/log` AND reads the JSON response to
   capture the DB `conversationId` (fires
   `AdapterInitOptions.onConversationPersisted`). Google's 3 posts + OpenAI's
   transcript/tool posts were rerouted through it; ElevenLabs captures inline to
   preserve its retry logic. **CHECK:** fire-and-forget semantics intact? any log
   post whose response isn't JSON handled (it `.catch`s)? did rerouting OpenAI's
   posts change any payload shape or drop a call site? (There are ~5 OpenAI log
   posts; only 2 were rerouted — confirm the others didn't need the id and still
   work.)

2. **Metering deferral** (`src/app/api/ai/tools/execute/route.ts`). `ctx.meter()`
   + reflink budget re-read moved to `next/server` `after()`; `costTracking`
   dropped from the response (verified no client reads it — the voice adapters
   read only `data`/`success`/`error`). **CHECK:** does ANY budget/rate
   ENFORCEMENT depend on the `tool_execution` ledger row being written
   synchronously? (It shouldn't — `costUsd:0`, enforcement is pre-handler in
   `withAIGateway` — but confirm the watchdog post-write cap re-check doesn't rely
   on it.) Confirm `after()` runs in this Next version at runtime (it type-checks;
   verify it actually fires).

3. **Shared component change:** `min-w-0` added to shadcn `SidebarInset`
   (`src/components/ui/sidebar.tsx`). Fixes a real horizontal-overflow bug (the
   fixed-width transcript panel grew the content area past the viewport) and
   affects ALL admin pages. **CHECK:** spot-check 2–3 other admin pages (media,
   semantic dashboard, rate-limiting) at ~1600px for no layout regression.

4. **Strengthened "speak BEFORE the tool" guidance** in the Gemini mint prompt
   (`src/app/api/ai/google/session/route.ts`). It's a longer `systemInstruction`
   string. This is a PARTIAL fix for the owner's silent-gap complaint; the
   GUARANTEED fix is task 9b (filler clips). **CHECK** the wording isn't
   counterproductive; ideally re-run one live Gemini fake-mic drive.

5. **Admin transcript browser** (new files: `ConversationBrowser`,
   `ConversationTranscriptPanel`, `ConversationAdminTools`, `ConversationsView` +
   `/api/admin/ai/conversations/browse`). Three-column layout (nav | list | full
   chat), collapsible analytics with at-a-glance totals + persistent Export modal
   + age-based cleanup. The Previous/Next stepper (`ConversationReplayViewer`) was
   REMOVED from admin and is reserved for a FUTURE homepage over-the-portfolio
   replay (not built). Legacy `conversation-management.tsx` is now
   UNIMPORTED/dead — consider deleting it. **CHECK:** browse route is admin-gated
   and its provider/model-from-latest-leg query is correct; replay route now
   accepts `conversationId` as well as `sessionId`.

**Known instrumentation gap** (documented, not yet fixed): the persisted
assistant-turn timestamp is turn-END (flushed on `turnComplete`), which masks the
true silence window during tool calls. Capture turn-ONSET (first audio chunk)
when you build 9b, so tool-latency telemetry is real.

**Not independently verifiable without a human:** audio QUALITY. Turn mechanics
are covered by the C0 fake-mic driver. The ONE remaining live-eyeball from last
session is the `conversationId` debug chip during a REAL voice session (it was
verified via the `/log` API return, not by watching the chip populate live).

Report review findings (bugs, risks, or "all clean") before proceeding.

---

## Phase 2 — Resume Phase 4 Block C  (order matters: 9 → 9b → AC8, then Block D)

Context: C0 (fake mic), 5b (D49 resume legs), and 6 (Gemini Live) are DONE. All
provider keys are fresh and live-fire verified (OpenAI, Anthropic current-gen
only, Gemini via `GEMINI_API_KEY`). Use the C0 fake-mic driver
(`/admin/ai/voice-debug` and the homepage, admin-gated) to e2e every voice task.

### Task 9 — Cascade voice family (D45), ai-assistant task 9
- New STT adapter surface (ElevenLabs Scribe / Deepgram / OpenAI transcription;
  keys env-only per D3, config in `VoiceProviderConfig`).
- `CascadeVoiceAdapter` behind `IConversationalAgentAdapter`: streaming STT →
  Block A reasoning adapter (the shared brain) → ElevenLabs streaming TTS.
  Modular: any LLM ±reasoning × any TTS; ElevenLabs is the quality default, NOT
  hardcoded.
- Push-to-talk or conservative VAD in v1 (barge-in is v2). Turn-taking is the
  known-hard part — keep it deliberately simple.
- Shared-brain check: same question via text vs cascade voice must yield the same
  grounded answer (scriptable with the C0 driver).
- Retire the ElevenLabs agent-platform adapter (D22 amendment) — ElevenLabs
  becomes TTS-engine-only.
- Admin A/B switch native ↔ cascade.
- Note: `design-voice-adapters.md` §2b now states the three modes explicitly
  (native s2s | text-only | cascade-on-top-of-text) — align to that wording.

### Task 9b — Pre-recorded voice clips (D50), ai-assistant task 9b
**This is the GUARANTEED fix for the silent-gap UX the owner reported this
session.**
- Client-side, adapter-independent clip player. `(voiceId, phraseId)`-keyed
  assets via the media domain, regenerated via provider TTS when the voice
  changes.
- Triggers: tool-latency filler with INSTANT cutoff when real speech arrives
  (this is the one that fills the dead air during tool calls), D49
  connection-state clips, optional cold-start greeting.
- `clip_played` history events, rendered distinctly in the transcript/replay.
- Also here: add turn-onset (first-audio-chunk) latency capture (see gap above).

### Access-and-cost task 8
- Voice session duration caps enforced at EVERY token mint.
- Reflink leak-containment backlog (a)+(b): first-N-IPs binding + per-IP
  sub-limits keyed `reflinkId:hashedIp`. Resolve the
  `reflink-status-indicator.tsx` manifest HOLD here.

### Then Block D (last)
About/AI page (architecture explanation, MCP URL + copy-paste client configs,
hardening checklist as prose, F-I-D/chunking highlights), README refresh
cross-checked against code, CLAUDE.md env update (`GEMINI_API_KEY`). Closes
mcp-server task 5. See `docs/article-notes-design-judgment.md` for narrative
angles.

---

Per-block: update every touched spec's ledger + "Last verified against code"
stamp, append owner design judgments to article-notes, and append any surprises
to `00-overview/phase3-cleanup-manifest.md`. Optional/time-boxed: D41 Gemini
tool-calling prototyping (only if it proves weak in cascade work).

STOP at any block boundary with everything green and committed rather than
starting work you can't verify. Finish with: what shipped, what live-fire proved
(and what the fake mic did/didn't cover), holds resolved, surprises appended,
ledgers updated.
