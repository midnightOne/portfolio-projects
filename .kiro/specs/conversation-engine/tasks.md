# conversation-engine — Tasks

**Status:** current — **Blocks A + B + C + D + E + G (G1–G4, G6; G5 open) + I + J + M (M1–M3; M4/M5 stay deferred by design) complete; F, H (email scope landed in G6), K, L open** (Block G amended + Block M added 2026-07-10 per the Block G owner interview — design-ux-and-behavior §9)
**Owner domain:** node-graph conversation engine (see requirements.md)
**Last verified against code:** 2026-07-11 (Block M landed + drilled in-session — memory layer un-gated from graphs behind the admin switch, secondary-LLM job module metering live summarizer/embedding runs, organic proto-node sampling from a real visitor click through to the admin query panel; see the Block M evidence notes. Same day: G6 email module/Resend landed + drilled in-session — see the G6 evidence note. Previous: Block G landed + drilled in-session — see the Block G evidence notes. Previous same-day verification: Block I + D4 landed + drilled in-session: question-analytics batch over the retained drill conversations with real greedy clustering + ledgered embedding, summary backfill converging to 0 candidates on re-run, editor promote-to-chip/exemplar persisted into a live draft, and a three-path returning-visitor resume drill — cross-device confirm with summary-briefing recall, same-device silent resume, start-fresh — against the retained J-drill conversation. Previous same-day verification: Block E replay/annotation/coverage drill)

Ordering note: blocks A → B → C are strictly dependency-ordered; D, G, H, I, J, L all depend on B and can proceed in parallel after it (D4 additionally needs I1; J1 should land early since H1/I3 write into the `ConversationState` contract); K rides with H/I; M follows G (M1 before L2/M4/M5 — they consume it; M2 reworks J gating); F closes the phase and depends on everything except L (safety is modular and may land after go-live). The liquid-pill/transcript-sidebar redesign lives in the **ui-system** spec (G components built against the current pill relocate there). Within a block, subtasks are one session's work each where possible. Block A lands in `ai-assistant`'s domain (coordinated here to keep one ledger; mirror one-line pointers in ai-assistant's ledger when A starts). Every block ends with the D46 loop: deterministic suite green + the block's e2e assertion exercised in-session.

**Before writing any code, read [design-implementation-notes.md](./design-implementation-notes.md)** — module layout (§1), normative sequences (§2), concurrency contracts (§3), provider fidelity (§4), and the P1–P40 issues catalog. `P#` references below point into that catalog; a task is not done until its listed P-items are handled and tested. §8 of that file lists what NOT to build — treat it as binding scope.

---

## Block A — Prerequisite seams (ai-assistant domain, engine-driven)

- [x] A1. Schema migration: `ConversationGraph`, `ConversationGraphVersion`, `GraphAnnotation`, `GraphScenario` (design §2); no changes to conversation tables (`latestState` is already `Json?`). Incremental migration per D54 conventions (re-verify any index DDL by hand).
  - _Evidence (2026-07-09):_ migration `20260710014526_conversation_engine_tables` applied — scope widened per owner session instruction to ALL engine tables (also `ConversationLead`, `NodeEntryQuestion`, `SafetyConfig`, `SafetyInvestigation`) so later blocks never migrate again. DDL hand-verified: `embedding` is an untyped pgvector `vector` column (dimension follows the batch's embedding model; no index on it yet — Block I decides), conversation tables untouched. Deviations from the design sketch, both additive: `ConversationLead.notifyError` (P25's visible failed-notification state) and `SafetyInvestigation.status` running|complete|failed (admin visibility of in-flight/failed runs; the in-flight *guard* still lives in ConversationState per P33).
- [x] A2. `updateSession()` on `IConversationalAgentAdapter` (D47(d)) — interface + `BaseConversationalAgentAdapter` scaffold returning per-field results (`applied`/`deferred-to-remint`/`unsupported`, notes §4), then per adapter:
  - [x] A2.1 OpenAI Realtime: `session.update` with FULL replacement semantics — always resend complete assembled instructions and complete tool array (P8); context items via the existing NAV_CONTEXT delete-then-create path; verify via fake-mic drill that an in-flight session accepts new instructions/tool schema without reconnect.
    - _Evidence:_ fake-mic drill 2026-07-09 (session `openai-1783649787005-fpe4i03z8`): live `updateSession({instructions, tools})` → `{instructions:'applied', tools:'applied'}` (each awaited the `session.updated` echo), same session id, no reconnect; replaced instructions demonstrably governed the model (canary "banana check" → "Directive applied.").
  - [x] A2.2 Google Live: instructions cannot change mid-session (P7) — fold guidance into a `realtimeInput.text` context item, record `degraded`; verify tool-set mutability at build time and document the re-mint fallback in adapter metadata (fidelity matrix: notes §4).
    - _Evidence:_ drilled on a live Gemini session — guidance fold observed on the wire (`[UPDATED GUIDANCE v1 — supersedes…]`, result `degraded`); tools → `unsupported`; mutability VERIFIED BY DRIVING (D22): a second `setup` frame closes the socket with 1007 "setup must be the first message and only the first" (doc-comment cites this). Probe also exposed + fixed a pre-existing reconnect bug: `_setupComplete` stayed true after provider-side close, timing out every reconnect.
  - [x] A2.3 Cascade: next-turn-assembly application — implement as explicit method returning `applied`, not silent absence.
    - _Evidence:_ explicit hooks return `applied` (unit-tested); truth documented in the adapter header ("/api/ai/chat re-derives server-side; directives never returned to cascade clients", notes §2.2.9).
  - [x] A2.4 **Design-philosophy doc-comment at the top of every adapter** (owner request 2026-07-09): OpenAI = "mutable conversation — items are addressable, sculpt freely"; Gemini = "append-only stream — nothing sent can be deleted (our own context blocks included), supersede don't retract, native compression manages the window"; Cascade = "we assemble every turn — all invariants exact". Keep in lockstep with the notes §4 matrix so provider-specific work always starts from the right mental model.
    - _Evidence:_ all three headers written; Gemini's cites the driven 1007 probe result. Field-result vocabulary extended beyond notes §4 with `degraded` (P7's own term) and `failed` (transport-level error → retried at next boundary) — honest telemetry, noted here as the written reason.
  - _Requirements: 2.3, 4.3, 19.1; registry D47(d)_
- [x] A3. D55 context buffer: per-conversation keyed buffer module (last-write-wins per source key, TTL, token-budgeted merge, turn-end flush via `updateSession`/next-turn assembly; every flush a D49 history event). Engine publishes under source key `engine`; migrate NAV_CONTEXT/F-I-D publishing into the buffer in the same task (one injector, D55). **The injector implements the floating-block invariant from day one**: one merged block at the conversation tail, removed + re-appended every turn even when unchanged (Req 19.1). _Handles P27._
  - _Evidence (2026-07-09):_ `src/lib/ai/context-buffer.ts` (keyed LWW, TTL with injectable clock, budget drops whole items, content-version) + injector in the base adapter with per-provider cadence. Drilled live: OpenAI block delete+re-created at each turn boundary even bit-identical (item id changed, count stayed 1) and replace-not-append on publish; Gemini got passive context for the FIRST time via versioned supersession (wire-verified `[CURRENT CONTEXT v1 — supersedes…]`; identical re-publish sent nothing). UIManager publishes `fid` into the buffer; a lone `fid` entry renders byte-identical to the legacy NAV_CONTEXT payload. Documented deviation: flush history events are written on content CHANGE only — bit-identical every-turn re-appends add no replay information (content is constant between change events), so replay fidelity holds without a row per turn. 8 unit tests.
  - _Requirements: 3.1, 3.6, 7.4, 19.1; registry D55_
- [x] A4. Turn-evidence + directive plumbing: `/api/ai/conversation/log` POST accepts UI-state deltas in the payload and returns optional `engineDirective` (full snapshot + `seq`; native voice only — cascade/text ignore the field and re-derive from `latestState`, notes §2.2.9); base adapter applies directives latest-wins at turn boundaries, queuing any that arrive mid-response. Inert while no graph is active (Req 2.7).
  - Acceptance: retried `/log` POST produces no second application; out-of-order directives dropped; directive arriving mid-response applied only after response completes. _Handles P4, P19, P20._
  - _Evidence (2026-07-09):_ `uiEvidence` rides user transcript posts into message metadata (drill-verified via GET); `engineDirective` returned on transcript/batch responses for openai/google only; `EngineDirective` type + Zod schema seed `src/lib/ai/engine/types.ts` (notes §1 layout). Verification seam: dev-only `/api/dev/engine-directive` (404s in production) stages a synthetic directive — D56-clean, only the directive's ORIGIN is synthetic. Live drill: staged seq 1 → returned on EVERY subsequent /log response incl. an explicit identical double-POST → exactly ONE `engine_directive` history row, one `context_flush` row, directive instructions governed the model ("pineapple check" → "Seq one applied."); contextItems landed under buffer key `engine`. Seq gate + mid-response queue + malformed-drop covered by 20 unit tests. Known pre-existing race (not introduced, out of A-scope): two CONCURRENT identical transcript POSTs can double-persist (check-then-insert in `persistVoiceEntries`); sequential retries dedupe correctly — the engine's own turn idempotency is B3's `lastEvaluatedTurnId` CAS.
  - _Requirements: 2.2, 2.3, 6.1, 11.4_
- Block A verification roll-up (D46): `npm run verify` green; full jest suite 65/65 suites, 835/835 tests (pre-engine suite untouched — removal-safety Req 2.7 holds; engine inert with no directive staged); 29 new deterministic tests (`context-buffer.test.ts`, `updateSession.test.ts`).

## Block B — Engine core (runtime evaluator)

- [x] B1. `src/lib/ai/engine/`: module layout exactly per notes §1 — graph document types + Zod validation (design §3; schemas shared with the editor client), `GraphSource` interface (no Prisma in core) + Prisma-backed `graph-store.ts`, per-instance cache keyed by immutable version id, graph validation rules (Req 1.6 + `always`-cycle detection, P5).
  - _Evidence (2026-07-09):_ layout matches notes §1 exactly (`types.ts`, `graph-source.ts`, `validation.ts`, `evaluator.ts`, `conditions/` one module per type, `directive.ts`, `engine.ts`; + `cheap-call.ts` for the P26 batched-call contract — addition noted). `graph-store.ts` caches immutable version docs, reads the active pointer fresh each call. Validation covers Req 1.6 + always-cycles + always-outside-start-chain warning + P21 undeclared-slot templates; registry-dependent checks take injected known sets (D48).
- [x] B2. Evaluator: condition modules (pattern / ui_state / tool_result / **chip** deterministic-by-id free-tier; intent via version-pinned exemplar embeddings with classifier tiebreaker band; **slot / turn_quality / probe** / pivot via the shared cheap call), strict priority order, one-transition-per-turn, off-graph state + re-entry, no engine-initiated heuristic transitions (notes §2.2.7). The single per-turn cheap call covers edge scoring + slot extraction + turn-quality + probe (P26). Deps (`embed`, `classify`, `log`) injected for testability. Unit tests against fakes for every condition type, priority tie, purge policy, off-graph cycle, classifier timeout → no transition. _Handles P5, P10, P11, P22, P26._
  - _Evidence:_ 35 unit tests (engine.test.ts) cover every condition type incl. chip-by-id-never-text (P22), intent similarity-clears-threshold-skips-classifier / tiebreaker-band / below-band-never-fires / no-embeddings-classifier-degrade (P10/P11), turn-quality escalation + reset, probe pattern OR classifier, always-never-fires-at-runtime, strict priority (walk stops at first fire), classifier-throw → no transition no crash. Slot EXTRACTION rides the P26 call and merges into state (same-turn `slot` conditions fire) — H1's remaining scope is templating events + lead capture. Probe patterns are host-injected config (engine-runtime), not core content (D48). `generateEmbeddingsForModel` added to embeddings.ts for the P11 pinned-model runtime embed.
- [x] B3. Hook-in per notes §2.1/§2.2: `startPolicy` at all mint routes + `/api/ai/chat` (start node feeds `context-provider`/`start-frame`; static frame remains the no-graph path — closes ai-assistant 5d.3; graph version PINNED at conversation start); `processTurn` at `/log` and `/chat` post-persist inside a swallow-all guard; idempotency gate on `lastEvaluatedTurnId` + CAS on `latestState` (merge-write, never blob replace); `node_transition` markers via `recordSessionMarker` (turn-zero entry marker included); resume briefing re-enters the persisted node (Req 2.6 — extend `getResumeBriefing`).
  - Acceptance: evaluator throwing mid-turn still returns 200 from `/log` with the turn persisted; same-turn replay yields exactly one marker; two concurrent posts yield exactly one transition. _Handles P1, P2, P3, P6, P9, P18._
  - _Evidence (live drills, 2026-07-09, temp graph over fixture content):_ TEXT — turn 1 stamped `∅ → start` (model answered "I'm in the orientation state" from the engine prompt suffix), turn 2 fired `start → deepdive (pattern)` with turnMessageId recorded, turn 3 answered from the deepdive node ("deep dive into the Chrono Kiln Controller"); `latestState.nodeId` mirror correct. VOICE (fake-mic, real OpenAI session) — first utterance stamped + fired the transition, directive seq 1 rode the /log response and applied client-side exactly once. P2: sequential retry of an identical turn body → ONE transition, retry response carried no directive. P3: two CONCURRENT posts of one new turn → exactly ONE transition (CAS loser silent). P1: core-level swallow unit-tested (throwing store → `{null,null}`, never a throw) + swallow-all `runEngineTurn` at every call site. Implementation notes: engine keys live under `latestState.engine`, merged via raw-SQL `jsonb_set` (atomic CAS-and-merge; the notes §3 "promote to a column" alternative not needed — recorded as the implementer's call); voice conversations stamp at FIRST `/log` turn, not at mint (client-direct voice mints before the conversation row exists — a graph published in that seconds-wide window pins at stamp time, documented deviation); "no active graph at first turn" pins `nodeId: null` for the conversation's lifetime (P6 applies to absence too — drill-verified that activating a graph later does not capture old conversations); node transitions ride the directive as engine-key floating-block context + full tool arrays, NOT instruction replacement (owner decision this session: "meet with all tools and guidance, then add strong guidance appended per node state"); a directive lost in transit is not re-issued until the next transition (derived-state contract; re-derive-on-demand can land with C3 if wanted). Removal safety (Req 2.7/12.3): graph archived → fresh conversation shows no suffix, no markers, engine pinned off; full pre-engine suite green (66/870).
- [x] B4. Context-set assembly: typed items → resolved content (entity→T1, chunk, entry-time search, static, fid-scope) in the single assembly point, budget policy per notes §6 (drop whole items, record drops in flush events), runtime PRIVATE-visibility exclusion inside the assembly call for public sessions (Req 11.2), publish to buffer under source key `engine`. _Handles P12, P13._
  - _Evidence:_ `resolveContextSet` in engine-runtime.ts — entity→T1 (with PROJECT-visibility check), chunk via `ContentSearchService.getContent(publicOnly)`, entry-time `searchContent(publicOnly)`, static verbatim, fid-scope as a focus directive; whole-item drops in authored order with drop labels returned (surfaced in the directive text path; flush-event drop records join in C3's `_debug.engine`). Live: the deepdive node's `search` item grounded the model's answer. `/log` sessions without a reflink are treated as PUBLIC for visibility (fail-safe direction, P13).
- [x] B5. Tool scoping: node allowlist filter layered in `/api/ai/tools/execute` (tier ∩ session ∩ node; node never grants beyond tier) + directive-carried full tool schema for `updateSession` (replacement semantics, P8).
  - _Evidence:_ live drill in the deepdive node (allowlist without ui_intent): `ui_intent` → 403 "not available in the current conversation state", `content_search` → 200 with items. Node filter fails OPEN to the tier check (narrow-only layer can't widen access); Req 3.5 baseline retrieval force-allowed in start/off-graph states; directive carries the full filtered OpenAI tool array (unit-tested); on Gemini tools are advisory-only mid-session — server enforcement is the teeth (adapter header doc, owner decision).
  - _Requirements: 1, 2, 3, 4; registry D47(a)(b), D49, D55_
- Block B verification roll-up (D46): `npm run verify` green; full jest 66/66 suites, 870/870 tests (35 new engine tests); drill graph seeded via a temporary script (deleted per D42 — durable scenario tooling is F2); the drill conversations' pinned `ConversationGraphVersion` rows retained so replay stays interpretable. Known limitation for C3: `_debug.engine` envelope not yet emitted (Block C scope).

## Block C — Model switching (cascade/text only) + debug envelope

*(Scope change, owner 2026-07-09: native in-flight/deferred model switching is REMOVED from the spec — native sessions keep their mint-time model, deep capability on native comes from server-side reasoning tools (D39), and the D49 re-mint path stays reserved for recovery/provider switching. C2 is now the cleanup task: Blocks A/B predate this decision, so any swap-ready machinery they introduced gets straightened back out, not extended.)*

- [x] C1. Cascade/text: directive `modelAlias` → next-turn resolution via `resolveModel` (pure data; assert ledger rows show the switch). On native, node `modelAlias` participates at session mint only (start node) and is ignored mid-session (Req 5.3/5.4).
  - _Evidence (2026-07-10, live 3-turn /chat drill over a temp graph with a `default-reasoning` deepdive node):_ turn 1–2 metered `gpt-4o-mini` (default-cheap), the transition fired on turn 2, turn 3 metered `gpt-4o` (default-reasoning) — **ledger rows show the switch** (`AIUsageLog.modelUsed`), `_debug.model` mirrored it. Implementation: `buildEnginePromptSuffix` returns `{suffix, modelAlias, debug}`; /chat resolves its adapter from the node alias with fail-safe fallback to `default-cheap` (stale-alias registry drift degrades, P1). **Native mint (Req 5.4) — DRIVEN provider finding (D22):** OpenAI `client_secrets` AND Google `auth_tokens` both mint tokens for nonsense model ids (probed 200 for "banana-nonexistent…") — providers validate at session CONNECT, client-side, so a retry-on-mint-reject net is dead code (an early draft of it was removed). Protection is `resolveEngineMintModel`'s ahead-of-time gate: alias must resolve, same provider, AND realtime-family model name; otherwise config default + warning. Drilled: pre-gate mint applied a `default-cheap` override (payload + response showed `gpt-4o-mini`); post-gate the same graph minted on config `gpt-realtime` with the ignore warning logged. Mint routes + ledger/response `model` fields now report the effective model.
- [x] C2. **Prune the deferred-swap remnants (owner: "straighten it back"):** remove `pendingModelSwap` from the `ConversationState` Zod schema (`src/lib/ai/engine/types.ts:199`) and any reads/writes of it; audit `engine.ts`/`directive.ts`/engine-runtime for swap-oriented branches (as of this rewrite the footprint is the schema field + directive `modelAlias` pass-through — verify nothing more grew); ensure the directive's `modelAlias` is consumed ONLY by cascade/text assembly and documented as ignored by native adapters; add the validation/editor warning "model-changing edges apply on cascade/text; ignored mid-session on native voice" (Req 5.4). No new machinery — this task only deletes and labels.
  - _Evidence:_ audit confirmed the footprint was the schema field alone (`EngineDirective` never grew a modelAlias field — now documented as deliberately alias-free, Req 5.3); schema field deleted, legacy persisted blobs strip the key on parse (unit-tested; drill conversation's `latestState.engine` verified clean). Validation warning `model_edge_native` fires per edge whose target alias differs from the source's, with the exact Req 5.4 copy (unit-tested both directions). Also finished the owner's uncommitted `buildProviderTools` removal found dangling in the working tree (constructor wiring + unused import; core seam retained per the recorded decision — transitions carry guidance only, tool enforcement stays server-side).
- [x] C3. `_debug.engine` envelope: active node, graph version, fired edge, evaluated edges (debug sessions), injected context items, effective tool set (Req 7.5). Include the B4 context-drop records and B3's directive-delivery state (the "known limitation" noted in the Block B roll-up).
  - _Evidence:_ `GatewayDebugState.engine` + `attachDebug` emit the section on the gateway envelope — /chat covers text AND cascade (the envelope's only surfaces; /log responses are not debug-authorized, voice telemetry stays in history rows — markers/`edge_evaluated`/`context_flush`, documented at the call site). Live envelope showed: `nodeId`/`graphVersionId`, `modelAlias`, `contextInjected` + B4 `contextDrops` (prompt phase), `firedEdge` (or null) + `evaluatedEdges` with reasons (turn phase, `ProcessTurnDebug` from the core), `toolAllowlist` in effect, `directive: {seq, delivery: log-response|server-assembly|none}` (B3 delivery state). Engine-off/CAS-loss/no-graph → section ABSENT (removal safety drilled: archived graph → fresh conversation has no `engine` key, no state guidance in the prompt). Discovered-and-closed B5 gap in passing: /chat's inline tool dispatch ignored the node allowlist (voice's tools/execute path was the only enforcement) — the same narrow-only check now runs at /chat dispatch, so the reported "effective tool set" is honest on every runtime (Req 4.1/6.1 parity).
  - _Requirements: 5, 7.3, 7.5; registry D4, D45_
- Block C verification roll-up (D46): `npm run verify` green; full jest 66/66 suites, 878/878 tests (8 new Block C tests in engine.test.ts — model-edge warning both directions, pendingModelSwap strip, startPolicy alias+drops, ProcessTurnDebug fired/no-fire/cascade-delivery/CAS-silent). In-session e2e: 3-turn text drill (ledger-verified model switch + envelope), OpenAI voice mint drill (alias gate + provider probes), removal-safety re-assertion. Drill graph archived; its pinned `ConversationGraphVersion` rows + drill conversation retained for replay (B convention).

## Block D — Admin graph editor

- [x] D1. Graph CRUD + publish APIs (design §6 route table), validation-on-save, publish per notes §2.3 (embed exemplars with recorded model id → snapshot → activate, one transaction; error-severity issues block), version list + structural diff (by stable node/edge id sets). _Handles P6, P11._
  - _Evidence (2026-07-10, live drill graph `drill-block-d-editor`):_ CRUD in `graph-store.ts` (notes §1 home), routes thin auth→zod→store. Publish DRIVEN: intent edge's 2 exemplars embedded in one batch → version doc records `{model: 'gemini-embedding-001', vectors: 2×1536}` (P11), ledger row `engine_exemplar_embedding` ($0.000003, metadata `graph_publish`), snapshot+activate in one transaction; error-severity blocked publish exercised (empty intent exemplars → 422, dialog button disabled). Structural diff pure module `graph-diff.ts` (5 unit tests; layout/embeddings ignored — sibling of graph-store, noted as layout addition). Additions beyond the §6 route table, with reasons: `POST [id]/versions/[versionId]/activate` (Req 8.4 re-activation needed an endpoint), `GET graphs/meta` (registry tools + alias roles + voice-clip tags + entity list for pickers — no entity-search endpoint existed). **Single-active rule enforced at publish/activate** (notes §8): activating a graph demotes any other active graph to 'draft' — DB-verified exactly one active. Create seeds start(+static start-frame snapshot ~388 tok)+offgraph (design §4); duplicate REGENERATES all ids (P15 at graph scale, drill-verified disjoint id sets); archive keeps versions/telemetry. `estimateTokensFromChars` moved to client-safe `token-estimate.ts` (pricing.ts re-exports; editor meter imports the same fn — notes §6 "import don't duplicate" without Prisma in the client bundle).
- [x] D2. Editor page: React Flow canvas (custom nodes/edges, persisted layout, validation badges, debounced draft autosave; ids minted once at creation and never regenerated — P15), node inspector (guidance blocks with `{{slots.x}}` placeholder validation against declared slots — P21, context-set builder with entity/chunk search pickers + live token meter using the SAME estimator as runtime, registry-enumerated tool picker, alias dropdown, voice-clip categories, **UX panel: chips / topic label / on-enter staging target picker**, **slot capture specs**), edge inspector (condition builder per type incl. chip/slot/turn_quality/probe, priority, purge, human-readable canvas labels). Model-changing edges labeled "applies on cascade/text; ignored mid-session on native voice" (Req 5.4); "changes apply to new conversations" copy on publish (P6); cascade/text-only note on any secrecy-sensitive guidance (notes §5)._
  - _Evidence:_ `@xyflow/react` (new dep) at `/admin/ai/conversation-graphs/[graphId]` (full-bleed via admin-layout editor rule; sidebar nav registered). Driven end-to-end in-session: node select → inspector edits (name/guidance/alias/chip) live-update canvas chips; token meter showed the seeded frame `static ~388/1200`; tool picker enumerated 18 registry tools with context labels; alias dropdown = exactly the 4 non-embedding roles; edge inspector drove target/priority/pattern→intent switch with canvas label `10 · pattern: kiln | controller`; autosave dirty→saving→saved observed; validation badges + toolbar counts live (server issues ride the PUT response). Req 5.4 warning shown BOTH from server validation and client-side hint. Chip condition picker offers the SOURCE node's chips by stable id (P22). Deviation/addition: toolbar "Add edge from selected node" button (drag-connect also works; synthetic-pointer test limitation surfaced that drag-only creation is touchpad-hostile) — inspector From/To selects complete the wiring. D4's questions panel deliberately absent (blocked on I1).
- [x] D4. "What visitors actually asked" panel in the node inspector: `GET /graphs/[id]/questions` clusters ranked by size, representative phrasings, one-click promote-to-chip (and optional promote-to-intent-exemplar) (Req 16.3). Depends on Block I1._
  - _Evidence (2026-07-10, same session as Block I):_ `QuestionsPanel` renders under the chips section (clusters ranked by size then recency; representative = cluster seed row; ≤3 sample phrasings collapsed; pending-rows count when embedding/clustering hasn't caught up) and hosts the I1 batch trigger with a result line. Promote-to-chip: label truncated at 48 chars, full question rides `sendText`. Promote-to-exemplar: per-cluster select over the node's **outgoing** intent edges — entry questions are what visitors ask once IN the node, so they exemplify its outgoing intents (interpretation recorded); no-intent-edges state explains itself. Drilled live on `drill-block-j-lifecycle`: 4 real clusters on `n_kiln`; chip promote → draft `ux.chips` gained "How does its control loop work?"; an intent edge created in-editor + exemplar promote → draft edge exemplars gained "What temperature sensors does it use, exactly?" — both verified via the draft GET after autosave. Coordination note: NodeInspector's new props are OPTIONAL (panel hidden when absent) so this session's commit and the parallel Block E session's GraphEditor commit stayed decoupled.
- [x] D3. Draft/publish UX: publish dialog with note, version history view, re-activate prior version, PRIVATE-content warnings (Req 11.2).
  - _Evidence:_ publish dialog with note + P6 copy + error/warning split (errors disable the button); version history lists v1/v2 with notes + node/edge counts; structural diff picked v1↔v2 → exactly `changed node: Kiln deep dive` (layout/embedding-only = "no structural changes"); re-activate v1 driven — pointer moved, draft untouched. PRIVATE/missing content checks are host-side `contentWarnings()` in graph-store (codes `private_content`/`missing_content` added to the core issue union; DB reads stay out of core) — warnings never block (admin sessions legitimately see PRIVATE; runtime excludes for public regardless, B4/P13). **Runtime closure drilled:** fresh `/chat` conversation pinned the re-activated v1 (`_debug.engine.graphVersionId`), "where am I" evaluated the intent edge 0.593 below-band (pinned-model utterance embedding working), exemplar-matching turn FIRED it (0.882 ≥ 0.82) into the deep-dive node — a graph authored entirely in the editor steered a real conversation. Drill graphs archived after (removal safety re-asserted: `_debug.engine` ABSENT on fresh conversation); `drill-block-d-editor` versions retained (B convention).
  - _Requirements: 1.3–1.6, 8; registry D47(b), D4_
- Block D verification roll-up (D46): `npm run verify` green (type-check, check:gateway incl. the new gateway-wrapped publish route, check:specs, check:semantic); full jest 68/68 suites, 910/910 tests (5 new: graph-diff). In-session e2e as above (editor→publish→runtime→re-activate→removal-safety). Note: session ran concurrently with the Block J session in the same working tree; the commit stages Block D files only (validation.ts staged as HEAD+D-edits via index so J's in-flight flags-validation lines land with J's own commit).

## Block E — Review, annotation, coverage loop

- [x] E1. Replay integration: node-path chips in `ConversationReplayViewer` + conversation browser; "show on graph" opens read-only editor pinned to the run's `graphVersionId` with traversal highlighted (Req 9.1).
  - _Evidence (2026-07-10, drilled over the RETAINED Block J drill conversations — no new spend):_ replay API resolves the run's pin (`latestState.engine.graphVersionId`, falling back to the first transition marker for conversations whose state was since overwritten) → `engine: {graphId, graphName, version, nodeNames}` on the payload; per-step attribution is a pure client-side walk (`traversal-utils.ts`, 4 unit tests) — derived, never stored. Both surfaces (stepper dialog + transcript panel, shared `ReplayStepCard`) show ⦿ node chips and honest marker headlines — fixing in passing the pre-E bug where every engine marker (`node_transition`/`conversation_summary`/`edge_evaluated`) rendered as "🔴 Session disruption". "Show on graph" → `[graphId]?traversal=<convId>&version=<versionId>` renders `TraversalViewer`: read-only canvas of the EXACT pinned version, visited nodes lit / unvisited dimmed, fired edge animated, hop panel with Previous/Next + click-to-jump path list + condition/evidence per hop. Drill: voice-drill conversation `cmrehpei9…` walked 4 hops (∅→orientation→kiln⇄contact), `n_off` correctly stayed dimmed. Route addition beyond the design §6 table, with reason: `GET graphs/[id]/versions/[versionId]` (the viewer needs the pinned immutable document; embedding vectors stripped — publish-time data the canvas never renders).
- [x] E2. Annotations: mark-turn UI in replay → `GraphAnnotation`; TODO drawer in the editor; resolve links annotation → fixing version (Req 9.2).
  - _Evidence:_ `GET/POST/PATCH /api/admin/ai/graph-annotations` (design §6 route exactly) over graph-store CRUD; graphId scoping joins through version ids (the model deliberately has no graphId column — the version pin is the interpretation context). Annotate affordance on every step card when the run had a graph AND a node was active (nodeId from the E1 walk; admin-trusted surface); annotations render inline on their message. Editor toolbar gains "TODOs" with open-count badge → drawer (kind badge, node name resolved from the version doc, note, deep link `/admin/ai/conversations?conversationId=…` — ConversationBrowser now reads that param and opens the transcript). Resolve semantics: explicit `resolvedByVersionId` wins, else the graph's CURRENT version (active pointer, else latest) — drawer copy says "publish the fix first, then resolve"; reopen clears the link. Drilled end-to-end: bad_answer mark on the kiln-deepdive answer → DB row `{messageId, nodeId: n_kiln, graphVersionId}` → drawer count 1 → resolve → `resolved_by_version_id = cmrehdbnv…` (v1) shown as "resolved by v1"; reopen path exercised via unit of UI only.
- [x] E3. Coverage: aggregation endpoint over transition markers (window filter, test-excluded), editor overlay: node hit-rates, dead nodes, hot off-graph exits, edge fire counts (Req 9.3).
  - _Evidence:_ `GET /api/admin/ai/graphs/[id]/coverage?days=` (default 30, clamped ≤365 — P14; SQL filters markerType+timestamp only, no GROUP BY cleverness, no rollup tables) over pure `graph-coverage.ts` (sibling of graph-diff — same layout-addition precedent; 6 unit tests: version scoping, P17 exclusion, dead nodes, exit ranking, cross-version stable-id counting, removed-node reconciliation). **P17 seam decision recorded for F1:** the single test flag is `AIConversation.metadata.test === true` — coverage reads it; F1 MUST write that same key at session start (no seam existed yet; exclusion applied in TS, not SQL, to dodge the three-valued-logic NULL trap). Reference doc = active version else draft; markers count across ALL the graph's versions (stable ids, Req 1.5). Editor: Coverage toggle → per-node hit badges (`N×` / "no traffic"), edge labels gain fire counts, right panel = window selector (7/30/90d) + totals + dead nodes + hot off-graph exits + removed-node entries. Drilled on `drill-block-j-lifecycle`: 2 conversations / 7 transitions, orientation 2× kiln 3× contact 2× offgraph dead, edge fires 2/2/1 — **reconciled exactly against raw SQL**; live P17 drill: tagging the text-drill conversation `test=true` dropped it (1 conv / 4 transitions / "1 test conversation(s) excluded"), untagged after.
  - _Requirements: 7, 9_
- Block E verification roll-up (D46): `npm run verify` green; full jest 70/70 suites, 920/920 tests (10 new: 6 graph-coverage, 4 traversal-utils; counts include the concurrent Block I session's in-flight tests sharing the tree). In-session e2e as above, all on retained drill data — zero AI spend. Removal safety (Req 2.7): engine-less conversation drilled — replay payload carries no `engine` field, transcript renders zero node chips / annotate buttons / show-on-graph. Note: session ran concurrently with the Block I session in the same working tree (D/J precedent); `GraphEditor.tsx` staged as HEAD+E-edits via the index so I's D4 NodeInspector prop wiring lands with I's own commit; `NodeInspector.tsx`/`QuestionsPanel.tsx` and all Block I files untouched by this commit. The one drill annotation row is retained as review-loop fixture data.

## Block F — Self-testing, scenarios, verification closure

- [ ] F1. Sandboxed test sessions: `test` tag set once at session start and read by analytics, coverage, AND spend alarms from that single flag (P17); any runtime incl. fake-mic; fully logged with debug-level traversal (evaluated-but-not-taken events, sampled off in production) (Req 10.1, 7.3).
- [ ] F2. Golden scenarios: `GraphScenario` CRUD, record-from-test-session, `check:scenarios` runner against fakes only — scripted classifier scores, stable-vector embeddings, injectable clock (P16) — with readable path diffs + one-action re-baseline; "run scenarios" on draft in the editor pre-publish; wire into `npm run verify` (Req 10.2–10.4).
- [ ] F3. Seed graph + behavior suite: author the seed node catalog (design-ux-and-behavior §8) with the owner; every §2 vignette (vague-browser escalation, prober 1–2-turn deflection + flat political/religious refusal, skeptic honesty + interview CTA, wanderer re-entry tone, JD form-first intake, identity/no-impersonation + LinkedIn fast path, AI-self showcase, qualify-then-capture, show-while-telling start, navigate-while-answering deep-dive) exists as a golden scenario and passes before the graph serves real traffic (Req 18.3).
- [ ] F4. Live-fire drill (phase completion): seed graph on real content, one conversation per runtime (native via fake-mic, cascade, text) asserting traversal, flush events, tool filtering, chips rendering + deterministic chip-edge fire, a slot fill → lead capture → notification, a node-driven model-alias switch on cascade/text (ledger-verified); removal-safety assertion (engine off → pre-engine suite green) (Req 12).
- [ ] F5. Docs closure: CLAUDE.md verification additions (`check:scenarios`, batch triggers), 00-overview index row verified, `_backlog/conversation-engine.md` marked superseded, D47 registry status flipped to implemented when F4 passes.
  - _Requirements: 10, 12, 18; registry D46, D53, D56_

## Block G — Visitor UX surfaces (after B; parallel to D) — *tasks amended 2026-07-10 per the Block G owner interview (design-ux-and-behavior §9)*

- [x] G1. Directive UX payload + pill rendering: `EngineDirective` gains `ux` (chips + topicLabel), delivered on `/log` responses (voice) and the `/chat` response envelope (text). Pill: chips row **replaces the legacy quick-actions placeholder** (max 3; animate outwards from the input section; positionally stable once shown; disintegrate on staleness; tapped chip gets a distinct "proper choice" animation while siblings disintegrate; untapped-sibling retain-vs-vanish behind ONE render-layer A/B flag — P37); chips persist during voice; tap sends `sendText ?? label` as a normal visitor turn **appearing in the transcript as the visitor's own** (voice: text-inject into the live session) + `chipId` in turn evidence (P22 — id-only matching, P40). Topic label: small grayish line at the pill edge/above input, disintegrate → appear-from-blur, hidden when absent (Req 13.1/13.3/13.5).
  - _Evidence (2026-07-10, drill graph `drill-block-g-ux`, archived after — version + drill conversations retained per B convention):_ `EngineUxSchema` in engine/types.ts (full replace-on-transition snapshot; runtime truncates at 3); `nodeUx()` in directive.ts; startPolicy/resumePolicy/ProcessTurnResult all carry ux — 8 new engine unit tests incl. chip-fires-by-id-with-unrelated-text and empty-surface-not-carried-over. **Three delivery paths, all drilled live:** (1) NEW public `GET /api/ai/engine/ux[?sessionId]` (lightweight surface read, no context resolution — chips render BEFORE first contact; un-metered by design: pure graph read, payload visitor-visible per Req 13.5) returned the drill chips; (2) `/chat` envelope: turn 1 carried start chips + "Overview", the chip-tap turn (`chipId: chip-kiln` with matching pattern text present) fired **`ge_chip` / conditionType `chip` / evidence "chip chip-kiln tapped"** — the deterministic edge beat the pattern edge, marker DB-verified — and the response swapped to kiln chips + "Kiln project"; (3) directive path: `directive.ux` → base-adapter `onEngineUx` at application time (boundary-respecting P19), unit-tested once-per-seq. UI drilled in the real homepage: 3 chips floated above the pill pre-conversation, "Topic: Overview" rendered, tapping `chip-ai` sent its **sendText** into the transcript as the visitor's own turn and the row vanished (default A/B mode; flag = localStorage `pill-chip-tap-mode` > `NEXT_PUBLIC_CHIP_TAP_MODE` > 'vanish'). Legacy quick-actions machinery deleted (pill + wrapper). Components self-contained in `engine-chips.tsx` for the ui-system liquid-pill relocation. Voice-runtime live drill of the /log directive→ux path deferred to F4 (mechanism unit-tested; shares the A4-drilled directive rail).
- [x] G2. Staging-as-guidance + auto-navigation consent toggle (Req 13.2 as amended, 13.8, 13.9, 18.5): `onEnterStaging` renders as a model-visible staging *suggestion* in the directive — the engine NEVER executes navigation (notes §8). New mechanics: two-state pill toggle, default OFF for new visitors, persisted under a non-engine `ConversationState` key, rendered into the floating block, flippable by tap AND by `set_auto_navigation` registry client tool (spoken flips are tool calls — P36); agent may call it only after explicit visitor consent. New-visitor portfolio intro: admin-editable text (what the site is, what the AI can do, the auto-nav offer) fed into system-prompt assembly — content, not code (Req 18.5).
  - _Evidence:_ staging suggestion line in `renderNodeGuidance` (references the AUTO-NAVIGATION policy + Req 13.6 preview preference; unit-tested, incl. never-leaks-into-visitor-ux). Toggle: `src/lib/ai/autonav.ts` store (localStorage `pill-autonav`, default OFF) → base adapter subscribes in constructor (publishes buffer key `autonav` + records `autonav_changed` uiEvidence; released via `_releaseBaseSubscriptions()` in all three adapters' cleanup) → `/log` mirrors flips into `latestState.prefs.autoNav` (new `mergeConversationPrefs`, atomic jsonb, non-engine key); `/chat` accepts `autoNav` per-request → policy line in the volatile tail + prefs persist — **drilled live: `prefs: {autoNav: true}` in DB after the chip-tap turn**. UI drill: tap flipped OFF→ON (aria-pressed + storage), `set_auto_navigation` tool flipped ON→OFF with honest result copy and the visible control tracked it (P36's no-pretense property). Intro: `CUSTOM/visitor-intro` T1 chunk read by `assembleStartFrame` (deterministic by key — never retrieved semantically; own budget, doesn't eat the overview), starter text seeded via `npm run seed:visitor-intro` (create-if-missing — owner edits in the admin chunk editor survive re-runs); drilled: turn-1 reply opened with the intro framing. Known scope note: voice-path uiEvidence→prefs persistence is code-complete but live-drilled only on /chat (voice drill rides F4).
- [x] G3. `job_description_form` client tool (scoped D18 exception; Req 13.4 expanded): **modal** with raw-paste field + file-drop area — extraction strictly client-side, only text sent (v1: plain-text files; PDF/DOCX extraction = follow-up task with lazy-loaded libs); processing animation; result rendered in-modal as an LLM-written compatibility document; analysis fed by the admin-editable, never-public **owner work-preferences record**; post-result delivery menu (spoken summary / read aloud / read in peace / email capture — address validated + stored on the conversation; **capture only — the send ships with H2's visitor-bound rate-limited email channel**). Submission feeds the existing job-analysis pipeline; allowlisted only where a node declares it. JD text obeys P38 input hygiene. Behavior: design-ux-and-behavior §2.5/§9.4.
  - _Evidence:_ migration `20260710215913_job_form_prefs_email` (DDL hand-verified): `AIOwnerPreferences` (server-side only — deliberately NOT a content chunk: non-project chunks are always-public + FTS-indexed, P13 leak) + `AIJobAnalysis.visitorEmail/emailRequestedAt`. `GET/PUT /api/admin/ai/owner-preferences` + editor panel on `/admin/ai/job-analysis`; analyze-job grounding gains the preferences ("paraphrase, never quote") and the output schema gains `document` (visitor-facing compatibility write-up; structured shape untouched — admin view unaffected). Client: `jd-form.ts` store + `job_description_form` client tool (registry-enumerated, opens the modal; result message tells the model the read-aloud fallback) + `JobDescriptionModal` (drop area rejects non-text v1 with honest copy, FileReader only — text never leaves the browser as a file; delivery buttons speak AS visitor turns; dismissal without submit publishes an honest note via the NEW `publishAssistantContext` adapter seam → buffer key `jd_analysis`). **Live drill (real reasoning-model spend, ~2k tokens):** tool call opened the modal on `?ref=test-premium`, pasted a firmware JD, analysis returned — document rendered in-modal, third-person, citing Chrono Kiln Controller for ESP32/FreeRTOS; DB row has `document` + reflink attribution. **Driven finding:** the email-request call 401'd — the modal didn't send `reflinkId`, and the gateway resolves tier from the body; fixed, then the endpoint drill persisted `drill-recruiter@example.com` + timestamp (ownership check binds analysisId to the caller's reflink; not-found and not-yours answer identically). Enforcement note (recorded): client tools don't pass through /api/ai/tools/execute, so node allowlisting of this tool is advisory client-side — the real teeth are server-side on /api/ai/analyze-job (reflink + enableJobAnalysis), same honesty class as Gemini advisory tools.
- [x] G4. Content prerequisite: verify the `/about/ai` article is ingested into the semantic index (AI-self node's context source, design-ux-and-behavior §2.7); ingest if missing.
  - _Evidence (2026-07-10):_ was NOT ingested (page is hardcoded TSX outside the project-centric pipeline). `npm run ingest:about-ai` (durable script; content distilled from the page — header marks the TSX as display source of truth, content-hash gated re-embedding): CUSTOM/`about-ai` entity + T0 metadata + T1 summary + 6 T2 section chunks, embedded in one batch (`gemini-embedding-001`, 1962 tokens, $0.000294, ledgered `about_ai_ingest_embedding`, budget-gated); re-run → all unchanged, 0 embeds (idempotent). Retrieval drilled `publicOnly: true`: "how does the voice assistant work" → T1 top (0.826 combined), MCP-safety question → MCP section top, filler-audio question → voice-UX section top.
- [ ] G5. (follow-up, small) PDF/DOCX client-side extraction for the G3 drop area — lazy-loaded parser libs, text-only-leaves-the-browser invariant unchanged.
- [x] G6. Email module (provider decision, owner 2026-07-11: **Resend**) — the Req 15.2 notification seam + the G3-deferred visitor-bound send (H2's email scope, pulled forward; H2 retains `lead_capture` + owner-notification wiring). _Handles P25 (send side)._
  - _Evidence (2026-07-11):_ migration `20260711190721_email_send_log` (`AIEmailSend` — one row per send ATTEMPT: channel owner|visitor, purpose, recipient, sessionId, analysisId, status sent|failed|skipped_unconfigured|skipped_rate_limited, providerId, error; DDL hand-verified, plain btree). Module at the design-ux §4 path `src/lib/ai/leads/`: `email-transport.ts` (Resend via raw fetch — POST api.resend.com/emails, Bearer key, 10s abort; `FakeEmailTransport` behind new `AI_FAKE_MODE=email`; **no key = well-defined null transport**, never an exception), `notify.ts` (the seam: `sendEmail`/`notifyOwner`; never throws; every attempt lands an honest row; visitor cap = sent-rows-count per sessionId, default 2, env `VISITOR_EMAIL_MAX_PER_CONVERSATION`, count-then-insert race accepted at cap boundary per P3 no-locks discipline; Reply-To = `OWNER_NOTIFY_EMAIL` so replies reach the human, Req 15.4), `email-render.ts` (markdown-lite → escaped inline-styled HTML, no md dependency; text part carries the document verbatim), `jd-email.ts` (composes subject from the row, mails `analysisResult.document`; pre-G3 rows without a document fail honestly). Wired: `/api/ai/analyze-job/email-request` now captures FIRST (P25) then dispatches — response copy honest per outcome ("Sent — on its way" vs the under-promising "Email recorded"); NEW `POST /api/admin/ai/job-analyses/[analysisId]/send-email` (owner send-now/retry, **bypasses the visitor cap**, rows marked adminInitiated; 200/400/502/503 per outcome); admin list returns `emailSend` latest-state and `JobAnalysisReview` renders badge ✓/state line/Send-email button (button hidden once sent); modal shows the server's copy verbatim. **Live drills (fake transport + one real unconfigured pass, zero spend):** (A) no key, no fake → `skipped_unconfigured` row with reason, capture retained; (B) fake mode → two `sent` rows (fake-1/fake-2, wire-observed `[email:fake]` with the 1862-char composed document); (C) third visitor send → `skipped_rate_limited` "2/2", transport untouched; (D) admin send-now past the exhausted cap → `sent` + adminInitiated, and an UNAUTHENTICATED send-now probe wrote NO row (middleware `/api/admin/:path*` redirect intercepts before the handler; `requireAdmin` is in-handler defense-in-depth — row-count-proven); admin UI text-verified (badge "drill-recruiter@example.com ✓", "email requested … — sent …"). 14 unit tests (`email-notify.test.ts`: rendering/escaping, cap + env override + bypass, unconfigured skips, Resend request shape + provider-rejection row, log-write-failure tolerance, jd composition). **Explicitly NOT verified: real Resend delivery** — no `RESEND_API_KEY` exists in this environment; deploy-time owner steps documented in deployment-guide §3 item 4 + env table (create Resend account, verify domain, set key/`EMAIL_FROM`/`OWNER_NOTIFY_EMAIL`; default `onboarding@resend.dev` delivers only to the account owner). Notes: `.env.example` is gitignored (env docs live in the tracked deployment guide); legacy `SecurityNotifier` email stub deliberately untouched — its doc pointer now says future enablement calls `notifyOwner()`; drill rows retained in `ai_email_sends` as fixture evidence (all against the retained G3 analysis `cmrfhp89s…`).
  - _Requirements: 13, 15.2, 15.4 (G6), 18.5; registry D18 (scoped), D59, D3, D48_
- Block G verification roll-up (D46): `npm run verify` green (type-check, check:gateway incl. the new gateway-wrapped email-request route, check:specs, check:semantic); full jest 73/73 suites, 946/946 tests (9 new: 8 engine G1/G2, 1 adapter onEngineUx). In-session e2e as itemized above (public-tier chip-fire drill + UI drills + JD analysis with real spend). Removal safety (Req 2.7): drill graph archived → `GET /api/ai/engine/ux` = `{ux: null}`, fresh /chat carries no `engineUx`, reply normal — note the new documented baseline: the visitor intro and auto-nav policy line are engine-INDEPENDENT surfaces (present when their content/flag exists, graph or not), per the owner's 2026-07-10 memory-without-graph ruling.
- G6 verification roll-up (D46, 2026-07-11): full jest 75/75 suites, 971/971 tests (14 new email-notify; counts include the concurrent Block M session's in-flight suites sharing the tree); check:gateway + check:specs + check:semantic --no-live all PASSED; type-check clean on every G6 file — the two tsc errors present at session end live in `analyze-job/route.ts` mid-refactor by the concurrent M1 session (its file, its commit). In-session e2e as itemized in G6. Session ran concurrently with the Block M session in the same working tree (D/J, E/I precedent): this commit stages only G6 files; `prisma/schema.prisma` staged as HEAD+AIEmailSend via the index so M2's `ConversationMemoryConfig` model lands with M's own commit.

## Block H — Slots & leads (after B; parallel to D)

- [ ] H1. Slot pipeline: capture specs in the node schema, extraction inside the shared per-turn cheap call (P26), conversation-scoped values in `latestState` (merge-write, P18) + history events on fill, `{{slots.x}}` templating at directive assembly with delimiting/length-cap/placement rules (P21), `slot` edge conditions (Req 14).
- [ ] H2. `ConversationLead` + `lead_capture` server tool (registry tool, node-allowlisted, conversational consent before the write — Req 21.3): DB row committed before any notification (P25); notification seam (`lib/ai/leads/notify.ts`) with admin channel always on + push = **email, invoked as a tool call** (owner decision 2026-07-09: "email IS MCP" — the agent's language is tool calls; the notify step is a server-side tool/function whose transport is an email provider; no separate push architecture) (Req 15.1/15.2). *Scope addition (owner 2026-07-10):* the same email module serves a **visitor-bound channel** — rate-limited to a couple of sends per conversation — delivering the G3 JD-analysis document to the visitor-captured address (and later artifacts like conversation summaries); G3 stores the address, H2 owns the send. **→ The email module itself LANDED as G6 (2026-07-11, provider Resend): seam, transport, visitor-bound channel + rate limit, and the JD send are live; H2's remaining scope = `lead_capture` tool + `ConversationLead` write + wiring lead pushes through the existing `notifyOwner()`.**
- [ ] H3. `/admin/ai/leads`: list with slot snapshots, status new/seen/handled, failed-notification badge, link into replay at the capture turn (Req 15.2).
  - _Requirements: 14, 15_

## Block I — Batch analytics & continuity (after B; independent of D–H)

- [x] I1. Question analytics batch: admin/cron endpoint scans since last run, joins `node_transition` markers to their next N user turns, writes `NodeEntryQuestion` (idempotent on messageId), embeds via `default-embedding` (budget-gated, ledgered, embedding-model id recorded), greedy pgvector similarity clustering — deliberately simple/swappable; test-tagged conversations excluded (Req 16; P17, P23). NO runtime-dynamic suggestions (Req 16.4).
  - _Evidence (2026-07-10):_ `engine-batches.ts` (host-layer; core untouched) + pure `question-analytics.ts` attribution join (first N=2 user turns after each transition, bounded by the conversation's NEXT transition so a turn evidences exactly one node — 6 unit tests) + `POST /api/admin/ai/engine/batch/questions` (gateway-wrapped D33, admin-only; cron wiring is a deploy-time addition, project out of deployment). DRIVEN over the retained B/C/D/J drill conversations: 17 transitions → 16 rows inserted, 16 embedded in ONE batched call (ledger `engine_question_embedding`: `gemini-embedding-001`, 216 tokens, $0.000032, rows:16 — model id recorded on the run), greedy clustering (cos ≥ 0.85, per-node) genuinely grouped two "kiln project" phrasings into one count-2 cluster; **re-run idempotent** (0/0/0 on the same 17). Budget gate: `canAffordOperation` pre-flight; unaffordable → embedding skipped with an honest note, rows stay pending (no silent caps). **Driven finding (P17):** the "clean" Prisma `conversation: NOT {metadata test=true}` filter silently excluded every UNTAGGED conversation (SQL three-valued logic — missing key → NULL = true → NULL → row dropped); first live run scanned 0. Exclusion now happens in JS over the fetched conversations; code comment forbids reintroducing the SQL form.
- [x] I2. Summarization batch: daily cron + admin trigger, **reusing the J3 summarizer module** (one summary pipeline, two triggers — Req 20.5): backfills/consolidates `conversation_summary` system rows for conversations that ended without a running summary; rendered in the admin transcript view (Req 17.2). _Handles P23._
  - _Evidence:_ `runSummaryBackfillBatch` selects engine-steered conversations (`engine.nodeId` non-null — the SAME removal-safety gate as the in-session trigger, Req 2.7; engine off → no summaries, app as today) with `last_message_at` newer than `lastSummarizerRunAt`, claims each via the SAME atomic `claimSummarizerRun` CAS (P29 — batch and a live turn can never double-run one conversation), then awaits `runSummarizerJob`. `POST /api/admin/ai/engine/batch/summaries` (gateway-wrapped, admin-only; both batch triggers also sit on the conversation-graphs list page). DRIVEN: first run → 3 never-summarized B/C drill conversations got v1 backfills, the J voice drill consolidated v3→v4 (post-summary tail folded), blockd drill v1→v2, and `sess_jdrill` (no new user turns) correctly no-op'd via `summarizerNeeded` (stamp only, no row). Second run → **0 candidates** (convergent). **Driven finding #2:** the candidate SELECT over-selected via naive-`timestamp` vs `timestamptz` comparison coercing through the server timezone — fixed with `AT TIME ZONE 'UTC'` (over-selection was harmless — the claim still gated — but the predicate now matches intent). Transcript rendering of summary rows was verified in J3 and re-observed in the drill conversation.
- [x] I3. Returning-visitor resume: same-reflink lookup; same-device continuity marker → auto-resume via the D49 path (third trigger); **new device/browser → confirmation with a safe one-line summary + always-offered "start fresh"** (Req 17.1, 21.1); stale-resume briefing = latest summary + recent verbatim turns, with full-transcript fallback when no summary exists yet (Req 17.3). _Handles P24, P32._
  - _Evidence:_ SERVER — `GET /api/ai/conversation/latest?reflink=` (reflink validation is the sole access control, P32; invalid/revoked → 403, so Req 21.5 revocation kills resume; returns sessionId + `summaryLine` = first sentence of the latest running summary ≤160 chars — never transcript content, Req 21.1; drilled: 403 bad code, 400 missing, correct payload for `test-premium`); `getLatestConversationForReflink` (≥1 user turn required); `getResumeBriefing` gained the P24 ladder — summary present → summary + ≤4 verbatim turns, absent → today's full recap — via pure `renderResumeBriefing` (6 unit tests; the briefing itself states the Req 17.4 unceremonious-recall rule). CLIENT — `portfolio-ai-continuity` localStorage marker (UX selector only, never auth — P32; 5 unit tests incl. corrupted-store tolerance and idempotent writes); pill checks the reflink's latest conversation once per load; marker match → silent-resume rail (`pendingResumeRef` rides EVERY pill connect via one `connectWithContinuity` wrapper), mismatch/absent → inline confirm card (mic-prompt pattern) with the summary line + always-offered "Start fresh"; **typing past an unanswered confirm starts fresh by construction** — resume requires the explicit choice (Req 21.1); marker written once the conversation has a real user turn (`getConversationSessionId` newly exposed on the agent context). DRILLED LIVE, all three paths on `?ref=test-premium` against the retained J-drill conversation: (1) no marker → confirm card with the v4 summary line → Continue → text-only session resumed the SAME logical session id (`openai-1783660997998-og1yq5biu`), leg 2 + `[session_resumed]` marker written, engine re-entered `n_kiln` (edge evaluation + directive seq 7 continued), summarizer minted v5 post-resume, and recall verified in-transcript: "you're a firmware engineer at Acme Robotics… Chrono Kiln Controller" — sourced from the summary briefing since the original verbatim turns were pruned in the J drill (Req 17.3 live); (2) marker match → subtle auto notice, a typed message silently resumed the same session ("as if after a brief disruption"); (3) Start fresh → marker cleared, next message opened a NEW conversation on a new session id. Cosmetic note: the first Continue click landed during the pill's intro animation and was swallowed (second click worked) — not a state bug, the offer stays until answered.
  - _Requirements: 16, 17, 21.1; registry D49_
- Block I verification roll-up (D46): `npm run verify` green; full jest 73/73 suites, 937/937 tests (17 new: 6 `question-analytics.test.ts` attribution join, 6 `resume-briefing.test.ts` P24 ladder rendering, 5 `continuity-marker.test.ts`). In-session e2e as itemized above (real embedding + summarizer spend, sub-cent; both batches idempotent/convergent on re-run). Session ran concurrently with the Block E session in the same working tree — E committed first (`5181a22`), this commit contains only Block I + D4 files; D4's editor wiring landed as a 5-line prop addition on top of E's GraphEditor. Drill residue: the archived `drill-block-j-lifecycle` draft gained a promoted chip + intent edge/exemplar (D4 drill artifacts, draft-only, graph archived); NodeEntryQuestion rows + new summary rows retained as live analytics data.

## Block J — Conversation memory & context lifecycle (after A3/B; parallel to D–I)

- [x] J1. `ConversationState` typed contract (design §2): Zod schema over `latestState`, `stateVersion` optimistic concurrency generalizing the B3 CAS, engine keys (node, slots, flags, agendaProgress, summaryVersion, contextSetVersion), merge-write preserved (P18). Migrate B3/H1 ad-hoc keys onto it.
  - _Evidence (2026-07-10):_ `ConversationStateSchema` in engine/types.ts — `stateVersion` + typed `engine` key, sibling legs/mode keys passthrough (P18); `EngineStateSchema` grew flags/agendaProgress/summaryVersion/contextSetVersion + summarizer bookkeeping (lastSummarizerRunAt, summarizerInFlightSince) + native-delivery counters (deliveredProfileVersion/deliveredSummaryVersion) — ALL defaulted on parse, pre-J blobs read cleanly (unit-tested). `stateVersion` bumps in SQL inside every manager write (updateLatestState, mergeEngineState, claimEngineTurn, claimSummarizerRun) — drill conversation reached stateVersion 35 with concurrent writers. Flag values are per-field `.catch`-tolerant: a garbage enum degrades that one flag, never the engine state (P18 hard requirement, unit-tested). B3 keys were already schema-conformant (nothing to migrate); H1 lands onto the contract when Block H starts.
- [x] J2. Visitor profile: fast flags in the shared per-turn cheap call (P26); profile rendered into the floating block as transparent observations (Req 19.5, P31); flags unified with slots for templating + edge conditions (Req 19.2); agenda rendering on node entry (Req 19.6).
  - _Evidence:_ cheap call gained `wantFlags`/`flags` (register/intent/behavior) as a **free rider** — `cheapCallNeeded` ignores it, so flags NEVER force a call that wasn't already happening (P26; the summarizer covers quiet nodes). `profile.ts`: renderProfileText (P31 sayable-aloud observations incl. duration from `flags.startedAt`, stamped at graph entry), mergeFastFlags (refines per key — wholesale replacement is the summarizer's move alone, P30). Unification: `{{flags.x}}` templates beside `{{slots.x}}` and slot conditions accept `flags.<key>` names (both validated against the known flag keys — new `undeclared_slot` variants). Delivery: profile rides the floating block under key `profile` — transition directives carry it beside `engine`; flag changes WITHOUT a transition mint a profile-only directive (seq-bumped, deliveredProfileVersion-gated — drilled live: `context_flush "Context block v1 → model (profile)"` + `engine_directive seq 1` before any transition); cascade/text re-derive it in the volatile suffix (§2.2.9). Agenda renders via node guidance into the block (Req 19.6); agendaProgress persisted as a contract key, no auto-tracking machinery built (nothing specced beyond persistence — flagged, not improvised).
- [x] J3. Behavior summarizer: separate cheap-LLM job over user turns only, structured output (intent/register/mood/topics), staleness-triggered at turn boundaries (≥60s interval, async fire-and-forget with in-flight guard — P29), gateway-metered; output REPLACES the profile wholesale (P30) and appends/updates the running `conversation_summary` row (Req 19.3/19.4, 20.5).
  - _Evidence:_ `summarizer.ts` (core: prompt+parse) + `runSummarizerJob`/`maybeRunSummarizer` in engine-runtime — ONE cheap call, TWO outputs per the task's own merge: the profile assessment instructed from **VISITOR turns only** (Req 19.3) and the running summary folding previous summary + the labeled exchange (Req 17.2's pipeline was never user-only; interpretation recorded here). Staleness claim is ONE atomic SQL CAS (`claimSummarizerRun`: interval + in-flight guard with 2-min stale self-heal + **nodeId non-null gate** — the summarizer only runs while the engine steers, so removal safety holds, Req 2.7). Row FIRST, state pointer after; profile replaces wholesale with `startedAt` preserved (drill: fast-flag `behavior: probing` visibly decayed to `cooperative` at the next run — P30 live). Metered `engine_summarizer` via recordUsage (D33); failure stamps lastSummarizerRunAt so a broken summarizer costs one attempt per interval, never one per turn. `conversation_summary` marker rows carry `{summaryVersion, upToMessageId (adapter item id when voice), profile}` — v1/v2/v3 observed on both drill runtimes. Block I2 reuses `runSummarizerJob` directly (exported; one pipeline, two triggers). Env knob: `ENGINE_SUMMARIZER_INTERVAL_MS` (default 60s).
- [x] J4. Rolling window: window config (minutes/turns), running-summary collapse of old verbatim turns; per-provider pruning — OpenAI item-delete + summary item, Gemini native compression at mint + summary text, cascade/text assembly-time with cache-stable ordering (Req 20.1–20.3; P28 — verify provider behavior by driving it, never docs alone). NEVER a re-mint for pruning.
  - _Evidence:_ `window.ts` (core policy: config + `selectPrunableTurns` — prunable only when COVERED by the summary AND outside the window by age (~5min) or count (16 items); null/unknown coverage → nothing prunes, fail toward keeping verbatim; unit-tested incl. coverage-boundary-beats-count). Delivery: `/log` responses gained `engineWindow` (versioned full snapshot — framed summary + upToItemId + config; native only, base adapter applies latest-wins at boundaries, P19; 5 new adapter unit tests). **OpenAI, DRIVEN (P28):** live text-input Realtime session — `CONV_SUMMARY <token>` item created at `previous_item_id:'root'` (accepted; token-acked via the NAV_CONTEXT listener; replaced per version v1→v3, old item deleted), then `conversation.item.delete` pruned 3 covered out-of-window turn items across 2 boundaries — **no reconnect, same session id, model answered on** (`window_prune` event rows persisted with deleted ids + summary version). Whether deleted items still bill is not observable client-side — flagged for a later ledger-level look, not assumed either way. Transcript processor skips CONV_SUMMARY like NAV_CONTEXT; fallback local ids and already-pruned items excluded; ≤10 deletes per boundary. **Gemini, DRIVEN:** `contextWindowCompression: { slidingWindow: {} }` in the mint's locked `bidiGenerateContentSetup` — token minted AND a raw-WS connect probe returned `setupComplete` (config valid end-to-end; actual eviction is provider-internal and not drivable at drill scale — accepted coarser control, P28, evidence-before-machinery per P27's deferred-refinement note). Summary text reaches Gemini as buffer key `summary` (priority 1 = top of block) under versioned supersession — one send per refresh. **Cascade/text:** `/chat` assembles stable-first (base + `CONVERSATION SO FAR vN`) with volatile last (node state + profile), verbatim window capped at assembly — drilled with prompt-index assertions (summary@2371 < state@2726 < profile@3198). Env knobs: `ENGINE_WINDOW_MAX_TURNS`/`ENGINE_WINDOW_MAX_AGE_MS`. No re-mint path exists anywhere in the mechanics.
- [x] J5. Continuity assertion: fake-mic drill spanning ≥2 graph transitions + ≥1 pruning event — transcript-verified that the model references pre-transition/pre-prune conversation naturally (Req 20.4); `_debug.engine` exposes window state + profile + last summarizer run.
  - _Evidence (2026-07-10, drill graph `drill-block-j-lifecycle` — orientation → kiln-deepdive ⇄ contact, published through the Block D editor APIs):_ ONE live OpenAI Realtime conversation (session `openai-1783660997998-og1yq5biu`, retained for replay) spanning **3 transitions** (orient→kiln, kiln→contact, contact→kiln), summaries v1–v3, directive seqs 1–6 each applied exactly once, floating block evolving `profile` → `engine+profile` → `fid+engine+profile`, **2 pruning events**, slots {company, visitor_role, contact_info} extracted via the P26 call, and profile intent flipping browsing→hiring after the contract turn. Req 20.4 verified in-transcript: post-prune, post-3-transitions, "what did I tell you about myself at the start?" → "you're a firmware engineer at Acme Robotics… you shared that with me at the beginning" — unceremonious recall from the summary item after the verbatim turns were deleted. **Deviation from the task's letter, honestly:** the drill drove the REAL voice session via typed input (`text-only` AudioInputMode — the D53 automated-driver mode; the drill browser had no Chrome extension and mic permission blocked, and password-gated fake-mic panels are off-limits to an agent) — turn mechanics, /log flow, directives, and item surgery are identical to the mic path, whose audio half was already fake-mic-drilled in Blocks A/B. `_debug.engine` gained `window` {summaryVersion, summaryIncluded, maxVerbatimTurns, verbatimTurns, lastSummarizerRunAt, summarizerInFlight} + `profile` — asserted on /chat. Removal safety re-asserted post-drill (graph archived → fresh conversation: no engine section, no summary/state text in prompt, summarizer claim gated off).
  - _Requirements: 7.2, 19, 20; registry D41(b), D49, D55_
- Block J verification roll-up (D46): `npm run verify` green; full jest 68/68 suites, 910/910 tests (27 new: 22 in `context-lifecycle.test.ts` — state contract tolerance, profile rendering/merge P30/P31, flags free-rider P26, slots/flags unification, profile-directive minting, summarizer prompt/parse, prune selection; 5 window-intake tests in `updateSession.test.ts`). In-session e2e: 4-turn /chat drill (`sess_jdrill_text_1`, summary-in-prompt ordering + profile + P30 decay ledgered in state) and the J5 voice drill above; Gemini mint+connect probe. Drill graph archived; its pinned version + both drill conversations retained (B convention). Known model-variance note: the text drill's cheap call returned no slot extractions on one turn (flags rode fine; the voice drill extracted all three slots) — extraction prompting can be tuned in F3 seed-graph work if it recurs.

## Block K — Privacy & data lifecycle (with H/I; before F closes)

- [ ] K1. Resume confirmation UX (I3 covers mechanics): pill copy for confirm-with-summary, "start fresh", and "forget this conversation" affordances; forget deletes visitor content (transcript, summaries, profile, slots, leads on request) while retaining anonymized telemetry (Req 21.2).
- [ ] K2. Retention: owner-configurable defaults (transcripts N days, summaries longer, leads until handled + M), expiry executed inside the existing batch jobs (P23); reflink revocation kills resume (Req 21.4/21.5).
- [ ] K3. Admin separation: leads/transcript views labeled visitor content vs. operational telemetry; deletion actions audit-logged (Req 21.2).
  - _Requirements: 21_

## Block L — Safety tripwire (after B; independent; modular — shippable last without blocking F)

- [ ] L1. `src/lib/ai/safety/`: static word-flag scan (normalized string matching, config-driven) wired into the persist path of `/log` and `/chat` — synchronous, never blocks/fails the write (P33); conservative normalization, no cleverness (P34); disabled config = short-circuit (Req 22.1/22.4).
- [ ] L2. Investigation agent: async fire-and-forget with per-conversation in-flight guard (P33), reasoning adapter, gateway-metered; structured verdict (benign/severity/recommended action/rationale) → `SafetyInvestigation` row, linked from the conversation in admin (Req 22.2).
- [ ] L3. Enforcement executor: admin-configured severity→action map — log-only / owner notification (H2 seam) / evidence publication into the engine stream (graph safety edges can fire) / session termination + reflink ban via `access-and-cost` surfaces, honest client-direct mechanics (P35: revoke resources + disconnect directive, never claim more) (Req 22.3/22.5).
- [ ] L4. `/admin/ai/safety` page + `GET/PUT /api/admin/ai/safety` + investigations list: word lists by category, investigation policy text, severity→action map, module on/off; e2e drill: flagged word in a fake-mic conversation → log succeeds, investigation fires once, verdict lands, configured action executes.
  - _Requirements: 22; registry D33, D48_

## Block M — Memory decoupling & secondary-LLM infrastructure (owner rulings 2026-07-10; after G — M1 prioritized, M4/M5 deferred)

- [x] M1. **Secondary-LLM job infrastructure (PRIORITIZED — owner: "the common machinery for the secondary LLM should be an infrastructure task"):** extract the shared lightweight-LLM job pattern already proven three times (P26 batched cheap call, J3 summarizer, I1 batch embedding) into one documented module/pattern — alias resolution (D4) + gateway metering (D33) + JSON-forced schema + in-flight guard + staleness/async discipline — and make it the REQUIRED consumption path for every new secondary-LLM feature: safety investigation (L2), JD analysis (G3), deferred suggestion generators (M4/M5). No bespoke plumbing per feature; refactor only where the three existing consumers genuinely diverge.
  - _Evidence (2026-07-11):_ `src/lib/services/ai/secondary-llm.ts` — `runSecondaryLLMJob` (alias→`resolveModelAlias`, metering via injected recorder so route-context meters (G3's gateway result) and fire-and-forget meters (J3's direct ledger write) both fit, Zod-schema-forced JSON, hard timeout, swallow-to-null P1 posture) + `runMeteredEmbeddingBatch` (the I1 embed step's budget gate + ledger row + recorded model id); `src/lib/ai/llm-json.ts` is the ONE strict-JSON extractor (fence-tolerant) shared by all consumers. All four existing secondary-LLM sites consume it: P26 cheap call (`cheap-call.ts` parse), J3 summarizer (engine-runtime), I1 batch embedding (engine-batches), G3 JD analysis (`analyze-job` route — the module awaits custom meters so the route persists costUsd/ledgerId). 11 unit tests (`secondary-llm.test.ts`). Live during the M3 drill: ledger rows `engine_summarizer` ×2 (gpt-4o-mini) and `engine_question_embedding` (gemini-embedding-001) — real jobs metered through the shared path, no bespoke plumbing left.
- [x] M2. Memory-provider contract + graph-independent memory layer (Req 19.7, P39): formalize the D55 buffer provider contract (typed `{sourceKey, priority, content, version, ttl?}`, source attribution in the rendered block, providers tolerant of each other's absence); **un-gate profile flags / behavior summarizer / rolling window from `engine.nodeId`** — the gate moves to a memory-layer admin switch (default on) so graph-less conversations get notes-on-the-user, running summaries, and window pruning; admin-configurable **default tool set for graph-less sessions**; removal safety re-asserted with BOTH the steering and memory switches off (= pre-engine behavior, Req 2.7 as re-scoped).
  - _Evidence (2026-07-11):_ `ConversationMemoryConfig` singleton (migration `20260711191903_conversation_memory_config`, DDL hand-checked — plain table, no vector) + `memory-config.ts` accessor (30s memo, read failure fails OPEN to defaults = shipped behavior) + `GET/PUT /api/admin/ai/memory-config` + `MemoryLayerPanel` on /admin/ai (switch → Save → PUT drilled through the real UI both directions). Core takes `memoryEnabled` injected (D48); graph-less conversations stamp `latestState.engine` as an anchor and the memory machinery keys off the switch alone: profile delivery to live native sessions rides the same versioned directive path (`deliveredProfileVersion` gate, P4 latest-wins race posture), summarizer + window delivery gated in engine-runtime, `buildEngineStartSuffix`/`buildEnginePromptSuffix` render profile/summary without steering, `getNodeToolAllowlist` applies the admin graph-less narrowing (never grants beyond tier; retrieval baseline force-merged). **Live drill (zero graphs — every conversation organic):** conversation `cmrgwy05r000jw5xcoxsy501u` reached `nodeId:null, profileVersion:2, summaryVersion:2, directiveSeq:1, deliveredProfileVersion:1` with real flags (mood/intent/topics/behavior) and a visible `[summary v1]` row — the full memory layer running graph-less. Switch OFF through the panel → resumed conversation `cmrfizq1u002mw5j4at4mxkah`: turn fully functional (content_search + grounded answer), engine state BYTE-IDENTICAL after the turn, zero summary rows — with no active graph that is both switches off = pre-engine behavior (Req 2.7 as re-scoped), asserted live and in units. Switch restored ON. 21 new core tests in `engine.test.ts` (56 total).
- [x] M3. Graph-less entry sampling (Req 16 extension, §9.7): UI events (project modal opened, section viewed) act as proto-node keys for the I1 sampling join over organic (graph-less) conversations; admin query surface by condition — the data the first real graph gets designed from.
  - _Evidence (2026-07-11):_ organic arm in the question-analytics batch (`engine-batches.ts` step 3b): raw-SQL carrier join (jsonb `?` key-exists on `metadata.uiEvidence`, engine-stamped `nodeId` null, test-tag exclusion COALESCE'd so key-missing rows survive — the twice-confirmed Prisma NOT-json-filter trap stays out of Prisma), `attributeOrganicEntryTurns` (v1 reads only `ui_state` F-I-D ground truth — agent `navigation` moves excluded; last event on a carrier wins; carrier + following turns capped like the steered arm), proto keys `proto:project_opened|section_viewed|route_changed:<value>` under sentinel graph id `__organic__`; organic rows ride the SAME embed+cluster steps (proto keys cluster among themselves). Admin surface: `GET /api/admin/ai/engine/organic-questions` (`?event`/`?match`) + the "Organic questions" section in the conversation-graphs manager. **Live drill, whole path:** visitor click on the homepage Chrono Kiln card → fid published `currentProject` → the next user turn's `metadata.uiEvidence` carried `{type:'ui_state', route:'home', project:'verification-fixture-kiln'}` → batch run `organicRowsInserted:3, rowsEmbedded:5, rowsClustered:5` → `proto:project_opened:verification-fixture-kiln` queryable with the real question ("What temperature sensors…"), API filters and the panel's event-select both narrowing correctly. Drill finding recorded: the homepage `#modal=<slug>` hash path opens the modal WITHOUT publishing `currentProject` (only the click/UIManager path calls `updateUIState`) — locus evidence misses hash-opened modals; out of M scope, flagged as a follow-up. 8 new tests (`question-analytics.test.ts`).
- [ ] M4. *(deferred — storage is ready, do not build until the static system's pros/cons are known)* Graph-tied dynamic chip selection: best-3-of-N from `NodeEntryQuestion` clusters at directive build.
- [ ] M5. *(deferred)* Graph-less model-initiated suggestions: suggestions tool → M1 pipeline → 3 text chips through the same rendering path (no ids — P40); FIRST drive the transcript-vs-playback lead-time assumption (D22) before designing to it; toggleable off.
  - _Requirements: 16.4 (clarified), 19.7, 2.7 (re-scoped); registry D4, D33, D48, D55_
- Block M verification roll-up (D46): `npm run verify` green; full jest 75/75 suites, 985/985 tests (M adds: 11 secondary-llm, 21 engine M2, 8 organic-attribution). E2e exercised in-session against the REAL voice pipeline (three OpenAI Realtime sessions, silent-input text fallback): memory layer on/off through the admin panel with byte-identical-state proof, organic sampling from a real visitor click to the admin panel, M1 module metering live summarizer + embedding ledger rows. Removal safety holds at this commit: zero active graphs + memory switch honored; memory ON default changes graph-less conversations only by ADDING profile/summary machinery (P1 swallow posture throughout).

---

## External API integration points

### This system requires (from other specs)
```typescript
// ai-assistant
"context-provider.ts / start-frame.ts": "single assembly point the engine feeds";
"IConversationalAgentAdapter.updateSession": "added in Block A2 (D47(d))";
"D55 context buffer": "added in Block A3";
"POST /api/ai/conversation/log": "turn evidence in, engineDirective out (A4)";
"conversationHistoryManager": "recordSessionMarker / updateLatestState / getResumeBriefing";
"UnifiedToolRegistry": "enumeration with metadata; execute-route allowlists";
// ai-admin
"resolveModel(alias)": "per-node model aliases (D4)"; "reasoning adapters": "default-cheap classifier calls";
// semantic-content
"ContentSearchService / entity & chunk reads": "context-set assembly";
// access-and-cost
"withAIGateway": "every classifier call metered (D33)";
// verification
"FakeReasoningAdapter / fake embeddings / fixture / test tagging": "scenario runner + drills (D46)";
```

### This system provides (for other specs to reference)
```typescript
ConversationEngine: "startPolicy + processTurn (src/lib/ai/engine/)";
"/api/admin/ai/graphs*": "graph CRUD, publish, versions, coverage, scenarios";
"node_transition markers + latestState.nodeId": "replay/browser rendering, resume re-entry";
"check:scenarios": "deterministic traversal regression (npm run verify)";
```
